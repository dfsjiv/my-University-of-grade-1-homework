# 员工管理系统 - 代码详细注释解释文档（下半部分）

> 接上半部分文档，继续对剩余源文件进行详细解释。

---

## 5. 现代化UI层（Modern UI）（续）

### 5.3 MainDashboard.java - 主仪表盘（续）

#### 面板切换机制

`showPanel()` 方法是整个仪表盘的核心调度方法，根据侧边栏按钮的 `actionCommand` 参数决定显示哪个功能面板：

```java
private void showPanel(String cmd) {
    contentPanel.removeAll();  // 清空内容区域
    
    switch (cmd) {
        case "employee":
            // 员工列表面板（懒加载）
            if (employeePanel == null) {
                employeePanel = new EmployeePanel(service, logService);
            }
            employeePanel.refreshData();
            contentPanel.add(employeePanel, BorderLayout.CENTER);
            break;
            
        case "add":
            // 添加员工（弹出对话框，不切换面板）
            showAddEmployeeDialog();
            return;  // 注意：这里 return 了，不会执行后面的 revalidate
            
        case "dept_stats":
            // 部门统计（懒加载 AnalyticsPanel）
            if (analyticsPanel == null) {
                analyticsPanel = new AnalyticsPanel(service, logService);
            }
            analyticsPanel.showDeptStats();
            contentPanel.add(analyticsPanel, BorderLayout.CENTER);
            break;
        // ... 其他 case 类似
    }
    
    contentPanel.revalidate();
    contentPanel.repaint();
    updateTotalLabel();
}
```

**关键设计点：**

1. **懒加载**：各功能面板在首次使用时才创建，节省内存
2. **面板复用**：创建后的面板对象被缓存，切换时不需要重新创建
3. **对话框模式**：添加员工、查询员工、转岗等操作使用对话框，不切换主面板
4. **状态更新**：每次切换面板后更新底部状态栏的员工总数

#### 时钟更新

```java
private void startClock() {
    clockTimer = new Timer(1000, e -> {
        String time = LocalDateTime.now().format(
            DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
        timeLabel.setText("🕐 " + time);
    });
    clockTimer.start();
}
```

- 使用 `javax.swing.Timer` 每秒更新一次时间显示
- 在窗口关闭时（`dispose()`）停止定时器，防止内存泄漏

#### 侧边栏按钮自定义绘制

侧边栏按钮重写了 `paintComponent()` 方法实现：
- **激活状态**：蓝色背景 + 左侧蓝色竖线指示器
- **悬停状态**：深灰色背景
- **按下状态**：更深的灰色背景

---

### 5.4 EmployeePanel.java - 员工管理面板

**文件路径**：`src/ui/modern/EmployeePanel.java`

**核心作用**：提供员工列表的表格展示、搜索、添加、编辑、删除功能。

#### 界面布局

```
┌──────────────────────────────────────────────────────┐
│ 👥 员工列表                                           │
│ 查看和管理所有员工信息                                 │
│                              🔍 [搜索框] ➕添加 ✏️编辑 🗑️删除 🔄刷新 │
├──────────────────────────┬───────────────────────────┤
│  ┌────────────────────┐  │  ┌─────────────────────┐  │
│  │ 工号│姓名│部门│...│  │  │  📋 员工详情         │  │
│  │ 001 │张三│技术部│  │  │  ━━━ 员工详情 ━━━     │  │
│  │ 002 │李四│市场部│  │  │  工号：001            │  │
│  │ 003 │王五│财务部│  │  │  姓名：张三           │  │
│  │ ...              │  │  │  部门：技术部         │  │
│  │                   │  │  │  ...                 │  │
│  └────────────────────┘  │  └─────────────────────┘  │
├──────────────────────────┴───────────────────────────┤
│ 共 X 条记录 | 显示 X 条                                │
└──────────────────────────────────────────────────────┘
```

#### 表格功能

**`styleTable()` - 表格样式配置**

```java
private void styleTable() {
    // 1. 设置字体、颜色、行高
    table.setFont(Theme.FONT_TABLE);
    table.setRowHeight(38);
    table.setShowGrid(false);
    
    // 2. 设置表头样式
    JTableHeader header = table.getTableHeader();
    header.setFont(Theme.FONT_TABLE_HEADER);
    header.setBackground(Theme.BG_CARD);
    
    // 3. 设置列宽
    table.getColumnModel().getColumn(0).setPreferredWidth(60);  // 工号
    table.getColumnModel().getColumn(1).setPreferredWidth(80);  // 姓名
    // ...
    
    // 4. 启用排序
    TableRowSorter<DefaultTableModel> sorter = new TableRowSorter<>(tableModel);
    table.setRowSorter(sorter);
    
    // 5. 自定义渲染器（斑马纹）
    table.setDefaultRenderer(Object.class, new DefaultTableCellRenderer() {
        @Override
        public Component getTableCellRendererComponent(...) {
            if (!isSelected) {
                c.setBackground(row % 2 == 0 ? BG_TABLE_ROW : BG_TABLE_ALT);
            }
            return c;
        }
    });
    
    // 6. 选中行显示详情
    table.getSelectionModel().addListSelectionListener(e -> showDetail());
    
    // 7. 双击编辑
    table.addMouseListener(new MouseAdapter() {
        @Override
        public void mouseClicked(MouseEvent e) {
            if (e.getClickCount() == 2) showEditDialog();
        }
    });
}
```

#### 搜索过滤

`filterTable()` 方法实现实时搜索：
1. 获取搜索框的关键字
2. 遍历所有员工，检查工号/姓名/部门是否包含关键字
3. 更新表格显示匹配的结果
4. 更新状态栏显示"共 X 条记录 | 显示 Y 条"

#### 详情显示

`showDetail()` 方法在右侧详情面板显示选中员工的完整信息：
- 基本信息：工号、姓名、部门、联系方式
- 类型信息：根据员工类型显示不同的薪资参数
- 入职信息：入职日期、工龄
- 绩效信息：当前绩效等级
- 薪资信息：计算后的薪资
- 请假记录：所有历史请假记录

---

### 5.5 EmployeeDialog.java - 员工对话框

**文件路径**：`src/ui/modern/EmployeeDialog.java`

**核心作用**：添加/修改员工时弹出的对话框，根据员工类型动态显示不同的薪资输入字段。

#### 动态表单

```java
private void updateSalaryFields() {
    salaryFieldsPanel.removeAll();
    
    String type = (String) typeCombo.getSelectedItem();
    
    if ("全职".equals(type)) {
        // 显示：基本工资 *
    } else if ("兼职".equals(type)) {
        // 显示：时薪 *、工作小时数 *
    } else if ("管理层".equals(type)) {
        // 显示：基本工资 *、奖金 *
    }
    
    salaryFieldsPanel.revalidate();
    salaryFieldsPanel.repaint();
}
```

当用户切换员工类型时，动态显示对应的薪资输入字段：
- **全职**：基本工资
- **兼职**：时薪 + 工作小时数
- **管理层**：基本工资 + 奖金

#### 输入校验

```java
private void saveEmployee() {
    // 1. 校验必填字段（工号、姓名）
    boolean valid = true;
    if (id.isEmpty()) {
        idField.setBackground(new Color(0x4A, 0x1C, 0x1C));  // 红色背景
        valid = false;
    }
    // 2. 校验数字格式
    // 3. 创建员工对象
    // 4. 调用服务层保存
    // 5. 记录日志
}
```

- 必填字段为空时，输入框背景变红色
- 数字格式错误时，显示错误提示
- 保存成功后关闭对话框，`isSuccess()` 返回 true

#### 编辑模式

当 `editEmployee` 不为 null 时，对话框进入编辑模式：
1. 预填所有字段的当前值
2. 工号字段禁用（不允许修改工号）
3. 保留原员工的请假记录

---

### 5.6 SalaryPanel.java - 薪资管理面板

**文件路径**：`src/ui/modern/SalaryPanel.java`

**核心作用**：提供薪资相关的所有功能：计算薪资、薪资排序、薪资筛选、薪资冠军、涨薪管理、请假扣薪。

#### 功能列表

| 方法 | 功能 | 说明 |
|------|------|------|
| `showCalculateSalary()` | 计算薪资 | 输入工号计算，显示所有员工薪资列表 |
| `showSalarySort()` | 薪资排序 | 从高到低排序，前三名显示奖牌 |
| `showSalaryFilter()` | 薪资筛选 | 输入最低/最高薪资范围筛选 |
| `showChampion()` | 薪资冠军 | 显示最高和最低薪资员工 |
| `showRaiseManagement()` | 涨薪管理 | 单个员工涨薪 + 部门涨薪 |
| `showLeave()` | 请假扣薪 | 选择请假类型和天数计算扣薪 |

#### 薪资排序实现

```java
public void showSalarySort() {
    List<Employee> all = service.getAllEmployees();
    // 使用 Collections.sort 按薪资降序排列
    Collections.sort(all, (e1, e2) -> Double.compare(e2.getSalary(), e1.getSalary()));
    
    int rank = 1;
    for (Employee emp : all) {
        // 前三名显示奖牌 emoji
        String rankEmoji = getRankEmoji(rank);  // 🥇🥈🥉
        // 第一名背景为金色
        row.setBackground(rank == 1 ? new Color(0x4A, 0x3A, 0x00) : Theme.BG_CARD);
        rank++;
    }
}
```

#### 薪资冠军显示

使用两个自定义卡片：
- **冠军卡片**：金色边框背景，显示 🏆 图标
- **垫底卡片**：红色边框背景，显示 📉 图标

两个卡片都通过重写 `paintComponent()` 实现自定义背景和边框。

---

### 5.7 AnalyticsPanel.java - 统计报表面板

**文件路径**：`src/ui/modern/AnalyticsPanel.java`

**核心作用**：提供数据统计与可视化图表：部门统计（饼图）、薪资柱状图、入职趋势（折线图）、月度报表。

#### 功能列表

| 方法 | 功能 | 可视化类型 |
|------|------|-----------|
| `showDeptStats()` | 部门统计 | 饼图（带标签和引导线） |
| `showBarChart()` | 薪资柱状图 | 柱状图（按薪资范围分组） |
| `showHireTrend()` | 入职趋势 | 折线图（按年份统计） |
| `showReport()` | 月度报表 | 文本报表 |

#### 部门饼图（DeptPieChart）

**内部类** `DeptPieChart` 继承 `JPanel`，通过重写 `paintComponent()` 自定义绘制饼图。

**绘制流程：**

1. **计算扇形角度**：根据各部门人数占总人数的比例计算每个扇形的角度
2. **绘制扇形**：使用 `Graphics2D.fillArc()` 绘制每个扇形
3. **绘制分隔线**：在扇形之间绘制背景色的分隔线，使边界清晰
4. **绘制中心文字**：在圆心显示"共X人"
5. **绘制标签和引导线**：
   - 从扇形边缘向外延伸引导线
   - 在引导线末端显示部门名、人数、百分比
   - 占比小于5%的不显示标签（避免拥挤）

**交互功能：**

```java
// 鼠标悬停时扇形突出（explode效果）
addMouseMotionListener(new MouseMotionAdapter() {
    public void mouseMoved(MouseEvent e) {
        hoveredIndex = getHoveredSector(e.getX(), e.getY());
        repaint();
    }
});
```

- 鼠标移动到某个扇形上时，该扇形向外偏移 10 像素（explode 效果）
- 通过计算鼠标位置与圆心的角度来判断悬停在哪个扇形上

**标签防重叠：**

```java
private int adjustLabelY(int proposedY, List<LabelPosition> existing, ...) {
    int y = proposedY;
    // 检测与已有标签是否重叠
    // 如果重叠，上下交替错开
    // 最多尝试 50 次
}
```

- 检测新标签与已有标签的垂直位置是否重叠
- 如果重叠，自动上下错开
- 确保所有标签都能清晰显示

#### 薪资柱状图（SalaryBarChart）

**内部类** `SalaryBarChart` 继承 `JPanel`，通过重写 `paintComponent()` 自定义绘制柱状图。

**薪资范围分组：**

| 范围 | 标签 |
|------|------|
| 0-5000 | 0-5K |
| 5000-10000 | 5-10K |
| 10000-15000 | 10-15K |
| 15000-20000 | 15-20K |
| 20000-30000 | 20-30K |
| 30000+ | 30K+ |

**绘制特点：**
- 柱体使用渐变色（从蓝色渐变到紫色）
- 柱体顶部显示人数数值
- 底部显示薪资范围标签

#### 入职趋势折线图（HireTrendChart）

**内部类** `HireTrendChart` 继承 `JPanel`，通过重写 `paintComponent()` 自定义绘制折线图。

**绘制流程：**

1. **数据统计**：按年份统计入职人数，使用 `Stream API` 的 `Collectors.groupingBy()` 分组
2. **绘制填充区域**：在折线下方填充半透明蓝色区域
3. **绘制折线**：使用 `Path2D.Double` 绘制平滑曲线（贝塞尔曲线）
4. **绘制数据点**：在每个数据点位置绘制圆形标记
5. **绘制标签**：在数据点上方显示人数，下方显示年份

```java
// 使用贝塞尔曲线绘制平滑折线
path.curveTo(
    (xPoints[i-1] + x) / 2, yPoints[i-1],  // 控制点1
    (xPoints[i-1] + x) / 2, y,              // 控制点2
    x, y                                     // 终点
);
```

---

### 5.8 SettingsPanel.java - 系统功能面板

**文件路径**：`src/ui/modern/SettingsPanel.java`

**核心作用**：提供系统维护功能：操作日志、导出CSV、批量导入、数据备份、恢复备份、修改密码。

#### 功能列表

| 方法 | 功能 | 说明 |
|------|------|------|
| `showLogs()` | 操作日志 | 显示所有操作记录，支持刷新和清空 |
| `showExport()` | 导出CSV | 导出员工数据为 CSV 文件 |
| `showImport()` | 批量导入 | 从 CSV/TXT 文件导入员工数据 |
| `showBackup()` | 数据备份 | 备份当前数据到 backup 目录 |
| `showRestore()` | 恢复备份 | 从备份文件恢复数据 |
| `showPassword()` | 修改密码 | 验证旧密码后设置新密码 |

#### 导出CSV实现

```java
private void exportCSV() {
    JFileChooser fileChooser = new JFileChooser(".");
    // 1. 弹出文件选择对话框
    int result = fileChooser.showSaveDialog(this);
    
    // 2. 写入 CSV 文件
    try (BufferedWriter writer = ...) {
        writer.write('\uFEFF');  // BOM for Excel（解决中文乱码）
        writer.write("工号,姓名,部门,联系方式,入职日期,薪资,绩效");
        // 逐行写入员工数据
        for (Employee emp : service.getAllEmployees()) {
            writer.write(String.format("%s,%s,%s,%s,%s,%.2f,%s", ...));
        }
    }
}
```

- 添加 BOM（`\uFEFF`）确保 Excel 正确识别 UTF-8 编码
- 使用 `JFileChooser` 让用户选择保存位置

#### 修改密码实现

```java
// 1. 验证旧密码
String currentPwd = readPasswordFromConfig();
if (!oldPwd.equals(currentPwd)) {
    JOptionPane.showMessageDialog(this, "当前密码错误", ...);
    return;
}

// 2. 保存新密码到 config.txt
try (BufferedWriter writer = ...) {
    writer.write(newPwd);    // 第一行：密码
    writer.newLine();
    writer.write("zh");      // 第二行：语言设置
}
```

- 密码存储在 `config.txt` 文件的第一行
- 第二行存储语言设置（"zh" 或 "en"）

---

## 6. 入口类

### 6.1 Main.java - 系统主入口

**文件路径**：`src/Main.java`

**核心作用**：作为整个系统的启动点，初始化各种服务组件，显示登录界面，启动主程序。

#### 启动流程

```
main()
  ├── 设置跨平台外观 (UIManager)
  ├── 初始化服务组件
  │   ├── LogService          (日志服务)
  │   ├── LanguageService     (语言服务)
  │   ├── EmployeeServiceImpl (员工服务)
  │   └── Scanner             (键盘输入)
  ├── 加载语言配置 (config.txt)
  └── 启动 GUI (SwingUtilities.invokeLater)
      └── ModernLoginView (登录窗口)
          └── 登录成功 → MainDashboard (主仪表盘)
```

#### 代码详解

```java
public static void main(String[] args) {
    // 1. 设置系统外观为跨平台外观
    // 确保 Swing 界面在不同操作系统上有一致的外观
    try {
        UIManager.setLookAndFeel(UIManager.getCrossPlatformLookAndFeelClassName());
    } catch (Exception e) {
        e.printStackTrace();
    }

    // 2. 初始化服务组件
    LogService logService = new LogService();           // 日志服务
    LanguageService lang = new LanguageService();       // 语言服务
    EmployeeService service = new EmployeeServiceImpl(logService);  // 员工服务
    Scanner sc = new Scanner(System.in);                // 键盘输入

    // 3. 加载语言设置
    loadLanguageConfig(lang);

    // 4. 启动现代化 GUI
    SwingUtilities.invokeLater(() -> {
        // 显示登录界面
        ModernLoginView loginView = new ModernLoginView(new JFrame(), () -> {
            // 登录成功后启动主仪表盘
            SwingUtilities.invokeLater(() -> {
                MainDashboard dashboard = new MainDashboard(service, logService);
                dashboard.setVisible(true);
            });
        });
        loginView.show();
    });
}
```

**关键设计点：**

1. **`SwingUtilities.invokeLater()`**：确保 GUI 创建在事件调度线程（EDT）中执行，避免线程安全问题
2. **回调机制**：`ModernLoginView` 接受一个 `Runnable` 回调，登录成功后自动执行
3. **嵌套 invokeLater**：登录成功后的仪表盘启动也放在 `invokeLater` 中，确保在 EDT 中执行

#### 语言配置加载

```java
private static void loadLanguageConfig(LanguageService lang) {
    File configFile = new File("config.txt");
    if (configFile.exists()) {
        try (BufferedReader reader = new BufferedReader(
                new FileReader(configFile, StandardCharsets.UTF_8))) {
            String password = reader.readLine();  // 第一行：密码
            String langSetting = reader.readLine();  // 第二行：语言
            if ("en".equals(langSetting)) {
                lang.switchLang();  // 切换到英文
            }
        } catch (IOException e) {
            // 读取失败则使用默认语言（中文）
        }
    }
}
```

- 从 `config.txt` 读取语言设置
- 如果第二行为 "en"，切换到英文模式
- 读取失败时静默处理，使用默认中文

---

## 附录

### A. 文件依赖关系图

```
Main.java
  ├── model/Employee.java
  │   ├── model/FullTimeEmployee.java
  │   │   └── model/Manager.java
  │   └── model/PartTimeEmployee.java
  ├── model/Performance.java
  ├── model/LeaveType.java
  ├── service/EmployeeService.java
  │   └── service/EmployeeServiceImpl.java
  │       ├── service/LogService.java
  │       └── service/LanguageService.java
  ├── ui/ColorUtil.java
  ├── ui/ConsoleUI.java
  ├── ui/LoadingAnimation.java
  ├── ui/LoginUI.java
  └── ui/modern/
      ├── Theme.java
      ├── ModernLoginView.java
      ├── MainDashboard.java
      ├── EmployeePanel.java
      ├── EmployeeDialog.java
      ├── SalaryPanel.java
      ├── AnalyticsPanel.java
      └── SettingsPanel.java
```

### B. 数据文件说明

| 文件 | 格式 | 说明 |
|------|------|------|
| `employees.dat` | 二进制（Java序列化） | 员工数据持久化文件 |
| `config.txt` | 文本（UTF-8） | 第一行密码，第二行语言设置 |
| `backup/backup_*.dat` | 二进制 | 数据备份文件 |
| `import_template.txt` | CSV（UTF-8） | 批量导入模板 |
| `employees_*.csv` | CSV（UTF-8 with BOM） | 导出的员工数据 |

### C. 关键算法复杂度

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按工号查找 | O(1) | HashMap 直接查找 |
| 按姓名查找 | O(n) | 遍历所有员工 |
| 按部门查找 | O(n) | 遍历所有员工 |
| 添加员工 | O(1) | HashMap 插入 |
| 删除员工 | O(1) | HashMap 删除 |
| 薪资排序 | O(n log n) | Collections.sort |
| 生成报表 | O(n) | 遍历所有员工 |
| 撤销操作 | O(1) | 从历史栈弹出 |

### D. 设计模式总结

| 模式 | 使用位置 | 说明 |
|------|---------|------|
| 模板方法 | Employee.getSalary() | 抽象方法由子类实现 |
| 策略 | Performance | 不同绩效对应不同奖金比例 |
| 工厂方法 | importFromFile() | 根据类型字符串创建员工对象 |
| MVC | EmployeePanel + EmployeeService + Employee | 视图-控制器-模型分离 |
| 观察者 | ListSelectionListener | 表格选中行时更新详情 |
| 适配器 | MouseAdapter/KeyAdapter | 简化事件监听器实现 |
| 单例（隐式） | EmployeeServiceImpl | 系统中通常只有一个实例 |

---

> **文档结束**
>
> 本文档对员工管理系统的所有源文件进行了详细的逐行解释，涵盖了架构设计、类层次结构、核心算法、UI 实现等各个方面。
> 如有任何疑问或需要进一步解释，请参考源代码中的注释或联系开发团队。
