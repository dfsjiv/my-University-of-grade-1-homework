# 员工管理系统 - 代码详细注释解释文档

> 本文档对项目所有源文件进行逐行级别的详细解释，帮助理解整个系统的架构、设计思路和实现细节。
> **注意：本文档仅为解释说明，不修改任何源代码。**

---

## 目录

1. [项目整体架构](#1-项目整体架构)
2. [模型层（Model）](#2-模型层model)
   - [Employee.java](#21-employeejava---员工抽象基类)
   - [FullTimeEmployee.java](#22-fulltimeemployeejava---全职员工类)
   - [PartTimeEmployee.java](#23-parttimeemployeejava---兼职员工类)
   - [Manager.java](#24-managerjava---经理类)
   - [Performance.java](#25-performancejava---绩效等级枚举)
   - [LeaveType.java](#26-leavetypejava---请假类型枚举)
3. [服务层（Service）](#3-服务层service)
   - [EmployeeService.java](#31-employeeservicejava---员工服务接口)
   - [EmployeeServiceImpl.java](#32-employeeserviceimpljava---员工服务实现)
   - [LogService.java](#33-logservicejava---日志服务)
   - [LanguageService.java](#34-languageservicejava---语言服务)
4. [控制台UI层（Console UI）](#4-控制台ui层console-ui)
   - [ColorUtil.java](#41-colorutiljava---颜色工具类)
   - [ConsoleUI.java](#42-consoleuijava---控制台交互界面)
   - [LoadingAnimation.java](#43-loadinganimationjava---加载动画)
   - [LoginUI.java](#44-loginuijava---Swing登录界面)
5. [现代化UI层（Modern UI）](#5-现代化ui层modern-ui)
   - [Theme.java](#51-themejava---主题配置)
   - [ModernLoginView.java](#52-modernloginviewjava---现代化登录窗口)
   - [MainDashboard.java](#53-maindashboardjava---主仪表盘)
   - [EmployeePanel.java](#54-employeepaneljava---员工管理面板)
   - [EmployeeDialog.java](#55-employeedialogjava---员工对话框)
   - [SalaryPanel.java](#56-salarypaneljava---薪资管理面板)
   - [AnalyticsPanel.java](#57-analyticspaneljava---统计报表面板)
   - [SettingsPanel.java](#58-settingspaneljava---系统功能面板)
6. [入口类](#6-入口类)
   - [Main.java](#61-mainjava---系统主入口)

---

## 1. 项目整体架构

### 1.1 三层架构设计

本项目采用经典的三层架构设计：

```
┌─────────────────────────────────────────────────┐
│                  UI 层（界面层）                    │
│  ┌──────────────┐  ┌──────────────────────────┐  │
│  │ ConsoleUI    │  │  Modern UI (Swing)       │  │
│  │ (控制台界面)  │  │  MainDashboard           │  │
│  │              │  │  EmployeePanel           │  │
│  │              │  │  SalaryPanel             │  │
│  │              │  │  AnalyticsPanel          │  │
│  │              │  │  SettingsPanel           │  │
│  └──────────────┘  └──────────────────────────┘  │
├─────────────────────────────────────────────────┤
│               Service 层（服务层）                  │
│  ┌────────────────────────────────────────────┐  │
│  │ EmployeeService   (接口)                   │  │
│  │ EmployeeServiceImpl (实现)                 │  │
│  │ LogService        (日志服务)               │  │
│  │ LanguageService   (语言服务)               │  │
│  └────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────┤
│               Model 层（模型层）                   │
│  ┌────────────────────────────────────────────┐  │
│  │ Employee (抽象基类)                        │  │
│  │  ├── FullTimeEmployee (全职员工)           │  │
│  │  │    └── Manager (经理)                   │  │
│  │  └── PartTimeEmployee (兼职员工)           │  │
│  │ Performance (绩效枚举)                     │  │
│  │ LeaveType (请假类型枚举)                   │  │
│  └────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 1.2 设计模式应用

- **模板方法模式**：`Employee` 抽象类定义 `getSalary()` 抽象方法，子类各自实现不同的薪资计算逻辑
- **策略模式**：`Performance` 枚举封装了不同的奖金比例策略
- **工厂方法模式**：`EmployeeServiceImpl.importFromFile()` 根据类型字符串创建不同的员工对象
- **MVC模式**：`EmployeePanel`（视图+控制器）通过 `EmployeeService`（模型）操作数据
- **单例模式**：虽然没有显式使用，但 `EmployeeServiceImpl` 在系统中通常只创建一个实例

### 1.3 数据流

```
用户输入 → ConsoleUI/MainDashboard → EmployeeService(接口) 
→ EmployeeServiceImpl(实现) → Employee(模型) → employees.dat(文件)
```

---

## 2. 模型层（Model）

### 2.1 Employee.java - 员工抽象基类

**文件路径**：`src/model/Employee.java`

**核心作用**：作为所有员工类型的基类，定义了员工共有的属性和行为。

#### 类定义与继承关系

```java
public abstract class Employee implements Serializable
```

- `abstract`：抽象类，不能直接实例化，必须由子类继承
- `implements Serializable`：实现序列化接口，使得员工对象可以通过 Java 序列化机制保存到文件（`employees.dat`）中

#### 序列化版本号

```java
private static final long serialVersionUID = 1L;
```

- 序列化版本号用于反序列化时验证类的版本一致性
- 如果类的结构发生变化（增删字段），需要更新此版本号，否则反序列化可能失败

#### 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `String` | 员工工号，唯一标识 |
| `name` | `String` | 员工姓名 |
| `department` | `String` | 所属部门 |
| `contact` | `String` | 联系方式 |
| `hireDate` | `LocalDate` | 入职日期 |
| `performance` | `Performance` | 绩效等级，默认 C 级 |
| `leaveRecords` | `List<String>` | 请假记录列表 |

#### 构造方法

```java
public Employee(String id, String name, String department, String contact, LocalDate hireDate)
```

全参构造方法，初始化所有基本信息字段。注意：`performance` 和 `leaveRecords` 有默认值，不在构造方法中初始化。

#### 抽象方法

```java
public abstract double getSalary();
```

- 抽象方法，强制子类实现各自的薪资计算逻辑
- 这是模板方法模式的核心，不同的子类有不同的薪资计算规则

#### 关键方法详解

**`getYearsOfService()` - 计算工龄**

```java
public long getYearsOfService() {
    if (hireDate == null) {
        return 0;  // 防御性编程，防止空指针异常
    }
    return ChronoUnit.YEARS.between(hireDate, LocalDate.now());
}
```

- 使用 `ChronoUnit.YEARS.between()` 计算两个日期之间的整年数
- 如果 `hireDate` 为 null，返回 0，避免空指针异常
- 这是防御性编程的典型例子

**`addLeaveRecord()` - 添加请假记录**

```java
public void addLeaveRecord(String record) {
    this.leaveRecords.add(record);
}
```

- 向请假记录列表中添加一条新记录
- 记录格式为字符串，包含请假类型、天数、扣薪明细等信息

#### 为什么使用抽象类而不是接口？

- `Employee` 有共享的字段（id、name 等）和实现的方法（getYearsOfService 等）
- 接口不能包含字段（Java 8 之前），而抽象类可以
- 子类之间是"is-a"关系（全职员工"是"一种员工），适合用继承

---

### 2.2 FullTimeEmployee.java - 全职员工类

**文件路径**：`src/model/FullTimeEmployee.java`

**核心作用**：表示全职员工，在 `Employee` 基础上增加 `baseSalary`（基本工资）字段。

#### 类定义

```java
public class FullTimeEmployee extends Employee implements Serializable
```

- 继承自 `Employee` 抽象类
- 再次 `implements Serializable`（虽然父类已经实现，但显式声明更清晰）

#### 特有字段

```java
private double baseSalary;  // 基本工资，全职员工的固定月薪基数
```

#### 薪资计算逻辑

```java
@Override
public double getSalary() {
    double base = baseSalary;
    return base + base * getPerformance().getRate();
}
```

**计算公式**：`薪资 = 基本工资 + 基本工资 × 绩效奖金比例`

- `getPerformance()` 从父类继承，返回当前绩效等级
- `getRate()` 返回绩效等级对应的奖金比例（A=20%, B=10%, C=0%）
- 例如：基本工资 10000，绩效 A 级（20%），则薪资 = 10000 + 10000 × 0.2 = 12000

---

### 2.3 PartTimeEmployee.java - 兼职员工类

**文件路径**：`src/model/PartTimeEmployee.java`

**核心作用**：表示兼职员工，在 `Employee` 基础上增加 `hourlyRate`（时薪）和 `hoursWorked`（工作小时数）字段。

#### 特有字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `hourlyRate` | `double` | 时薪，每小时薪资标准 |
| `hoursWorked` | `double` | 工作小时数，本月实际工作总时长 |

#### 薪资计算逻辑

```java
@Override
public double getSalary() {
    double base = hourlyRate * hoursWorked;
    return base + base * getPerformance().getRate();
}
```

**计算公式**：`薪资 = 时薪 × 工时 + (时薪 × 工时) × 绩效奖金比例`

- 先计算基础薪资（时薪 × 工时）
- 再在此基础上加上绩效奖金
- 例如：时薪 50，工时 160 小时，绩效 B 级（10%），则薪资 = 50×160 + 8000×0.1 = 8800

---

### 2.4 Manager.java - 经理类

**文件路径**：`src/model/Manager.java`

**核心作用**：表示管理层员工，继承自 `FullTimeEmployee`，增加 `bonus`（奖金）字段。

#### 类定义

```java
public class Manager extends FullTimeEmployee implements Serializable
```

- 继承自 `FullTimeEmployee`，因此间接继承了 `Employee` 的所有字段
- 继承链：`Employee → FullTimeEmployee → Manager`

#### 特有字段

```java
private double bonus;  // 奖金，经理在基本工资之外额外获得的固定奖金
```

#### 薪资计算逻辑

```java
@Override
public double getSalary() {
    double base = getBaseSalary() + bonus;
    return base + base * getPerformance().getRate();
}
```

**计算公式**：`薪资 = (基本工资 + 奖金) + (基本工资 + 奖金) × 绩效奖金比例`

- 与 `FullTimeEmployee` 的区别：计算基数包含了奖金
- 先计算基础部分（基本工资 + 奖金），再在此基础上加上绩效奖金
- 例如：基本工资 15000，奖金 5000，绩效 A 级（20%），则薪资 = (15000+5000) + 20000×0.2 = 24000

#### 继承层次的意义

```
Employee (抽象基类)
  └── FullTimeEmployee (全职：基本工资)
       └── Manager (经理：基本工资 + 奖金)
  └── PartTimeEmployee (兼职：时薪 × 工时)
```

这种继承结构体现了"is-a"关系：
- 经理"是"一种全职员工（所以继承 FullTimeEmployee）
- 全职员工"是"一种员工（所以继承 Employee）
- 兼职员工"是"一种员工（所以继承 Employee）

---

### 2.5 Performance.java - 绩效等级枚举

**文件路径**：`src/model/Performance.java`

**核心作用**：定义员工的绩效等级，每个等级对应不同的奖金比例。

#### 枚举常量

| 常量 | 奖金比例 | 含义 |
|------|---------|------|
| `A` | 20% | 优秀 |
| `B` | 10% | 良好 |
| `C` | 0% | 合格（无奖金） |

#### 为什么使用枚举而不是常量？

- 类型安全：只能使用 A/B/C 三个值，不能传入其他值
- 封装性：奖金比例与枚举常量绑定，不会出现不一致
- 可扩展性：可以轻松添加新的绩效等级

#### 关键方法

```java
public double getRate() {
    return rate;
}
```

返回奖金比例，在薪资计算时被调用。

---

### 2.6 LeaveType.java - 请假类型枚举

**文件路径**：`src/model/LeaveType.java`

**核心作用**：定义员工可申请的请假类型，每种假期对应不同的扣薪比例。

#### 枚举常量

| 常量 | 扣薪比例 | 含义 |
|------|---------|------|
| `ANNUAL` | 0% | 年假，带薪休假不扣工资 |
| `SICK` | 50% | 病假，只发一半日薪 |
| `PERSONAL` | 100% | 事假，无薪休假 |

#### 关键方法

**`getDeductionRate()`** - 获取扣薪比例
- 在 `EmployeeServiceImpl.applyLeave()` 中用于计算扣薪金额

**`getChineseName()`** - 获取中文名称
- 返回"年假"/"病假"/"事假"，用于界面显示

**`getRatePercent()`** - 获取百分比显示形式
- 将小数转换为百分比字符串（如 0.5 → "50%"）

---

## 3. 服务层（Service）

### 3.1 EmployeeService.java - 员工服务接口

**文件路径**：`src/service/EmployeeService.java`

**核心作用**：定义员工管理系统的所有业务逻辑接口，是 UI 层与实现层之间的契约。

#### 接口方法分类

| 类别 | 方法 | 说明 |
|------|------|------|
| 增删改查 | `addEmployee` | 添加员工 |
| | `removeEmployee` | 删除员工 |
| | `updateEmployee` | 更新员工 |
| | `findById` | 按工号查找 |
| | `findByName` | 按姓名查找（模糊） |
| | `findByDept` | 按部门查找 |
| | `fuzzySearch` | 模糊搜索 |
| | `getAllEmployees` | 获取所有员工 |
| 薪资 | `calculateSalary` | 计算薪资 |
| | `findBySalaryRange` | 按薪资范围筛选 |
| | `raiseSalary` | 单个涨薪 |
| | `raiseDeptSalary` | 部门涨薪 |
| 绩效 | `setPerformance` | 设置绩效 |
| 统计 | `getDeptStats` | 部门人数统计 |
| | `getHighestSalary` | 最高薪资 |
| | `getLowestSalary` | 最低薪资 |
| 操作 | `undo` | 撤销 |
| | `applyLeave` | 请假扣薪 |
| | `transferEmployee` | 员工转岗 |
| 数据 | `backupData` | 备份 |
| | `restoreFromBackup` | 恢复备份 |
| | `importFromFile` | 批量导入 |
| 报表 | `generateMonthlyReport` | 月度报表 |

#### 接口设计原则

- **接口隔离原则**：将不同职责的方法组织在一个接口中，但方法粒度适中
- **依赖倒置原则**：UI 层依赖接口而非具体实现，便于替换实现

---

### 3.2 EmployeeServiceImpl.java - 员工服务实现

**文件路径**：`src/service/EmployeeServiceImpl.java`

**核心作用**：实现 `EmployeeService` 接口的所有方法，是系统的核心业务逻辑所在。

#### 核心数据结构

```java
private final Map<String, Employee> employeeMap = new HashMap<>();
```

- 使用 `HashMap` 存储所有员工，key 为工号（String），value 为员工对象
- HashMap 的查找时间复杂度为 O(1)，适合按工号快速查找

#### 操作历史栈（撤销功能）

```java
private final Deque<Map<String, Employee>> history = new ArrayDeque<>();
private static final int MAX_HISTORY = 10;
```

- 使用 `Deque`（双端队列）作为栈，支持撤销操作
- 每次修改操作前保存当前数据的快照（深拷贝）
- 最多保留 10 步历史，超出时自动移除最旧的记录

#### 数据持久化

```java
private static final String FILE_PATH = "employees.dat";
```

- 使用 Java 对象序列化将数据保存到 `employees.dat` 文件
- `saveData()` 方法将 `employeeMap` 序列化写入文件
- `loadData()` 方法从文件反序列化还原数据

#### 关键方法详解

**`addEmployee()` - 添加员工**

```java
public void addEmployee(Employee e) {
    if (employeeMap.containsKey(e.getId())) {
        throw new IllegalArgumentException("工号 " + e.getId() + " 已存在，添加失败");
    }
    saveSnapshot();  // 保存快照用于撤销
    employeeMap.put(e.getId(), e);
    if (logService != null) {
        logService.addLog("添加员工：工号=" + e.getId() + "，姓名=" + e.getName());
    }
    saveData();  // 持久化到文件
}
```

流程：
1. 检查工号是否已存在
2. 保存当前数据快照（用于撤销）
3. 将员工添加到 Map
4. 记录操作日志
5. 持久化到文件

**`applyLeave()` - 请假扣薪**

```java
扣薪规则：
- 日薪 = 月薪 / 22（按每月22个工作日计算）
- 扣薪金额 = 日薪 × 天数 × 扣薪比例
- 实发薪资 = 月薪 - 扣薪金额
```

- 年假（ANNUAL）：扣薪比例 0%，不扣工资
- 病假（SICK）：扣薪比例 50%，扣一半日薪
- 事假（PERSONAL）：扣薪比例 100%，扣全部日薪

**`transferEmployee()` - 员工转岗**

转岗流程：
1. 检查员工是否存在
2. 检查是否转岗为同类型（不允许）
3. 保存快照
4. 根据目标类型创建新的员工对象
5. 复制公共字段（绩效、请假记录）
6. 用新对象替换 Map 中的旧对象

**`generateMonthlyReport()` - 月度报表**

报表内容：
- 按部门分组统计
- 每个部门：人数、最高薪资、最低薪资、平均薪资、薪资总额
- 全公司合计数据
- 本月人力成本

**`updateEmployeeSalary()` - 更新员工薪资（私有方法）**

由于 `Employee` 的薪资是通过 `getSalary()` 计算得来的，没有 `setSalary()` 方法，涨薪的实质是按比例调整底薪/时薪等基础参数：

- 全职员工：按比例调整 `baseSalary`
- 经理：按比例同时调整 `baseSalary` 和 `bonus`
- 兼职员工：按比例调整 `hourlyRate`

---

### 3.3 LogService.java - 日志服务

**文件路径**：`src/service/LogService.java`

**核心作用**：记录员工管理相关的操作日志，提供日志的添加和格式化打印功能。

#### 日志格式

每条日志格式为：`[yyyy-MM-dd HH:mm:ss] 操作内容描述`

例如：`[2026-06-04 14:30:25] 添加员工：工号=001，姓名=张三`

#### 关键方法

**`addLog()`** - 添加日志
- 自动添加当前时间戳

**`printLogs()`** - 打印日志（美化版）
- 使用 ANSI 颜色和 Unicode 边框字符美化展示
- 时间戳显示为青色，操作内容显示为白色
- 日志列表用黄色边框包围

---

### 3.4 LanguageService.java - 语言服务

**文件路径**：`src/service/LanguageService.java`

**核心作用**：提供中英文双语切换功能，所有提示语通过 key 获取。

#### 实现方式

- 使用两个 `HashMap` 分别存储中文和英文的提示语
- key 为字符串标识（如 "menu.add"、"prompt.input.id"）
- 通过 `currentLang` 字段控制当前语言

#### 提示语分类

| 分类 | key 前缀 | 说明 |
|------|---------|------|
| 菜单标题 | `menu.` | 主菜单项文字 |
| 输入提示 | `prompt.` | 用户输入提示 |
| 子页面标题 | `title.` | 各功能页面标题 |
| 成功/失败消息 | `success.` / `error.` | 操作结果提示 |
| 信息提示 | `info.` | 系统信息 |
| 员工类型 | `type.` | 员工类型名称 |
| 查询方式 | `query.` | 查询方法选项 |
| 绩效等级 | `performance.` | 绩效选项 |
| 请假类型 | `leave.` | 请假选项 |
| 表格表头 | `table.` | 表格列名 |
| 其他 | `other.` | 其他提示 |

#### 关键方法

**`get(String key)`** - 获取当前语言对应的文字
- 根据 `currentLang` 选择对应的 Map
- 如果 key 不存在，返回 key 本身

**`get(String key, Object... args)`** - 获取格式化文字
- 支持 `String.format()` 风格的参数替换

**`switchLang()`** - 切换语言
- 在 "zh" 和 "en" 之间切换

---

## 4. 控制台UI层（Console UI）

### 4.1 ColorUtil.java - 颜色工具类

**文件路径**：`src/ui/ColorUtil.java`

**核心作用**：提供 ANSI 转义码的封装，用于美化控制台输出。

#### ANSI 转义码

ANSI 转义码是控制台文本格式化的标准方式，格式为 `\u001B[XXm`：
- `\u001B[0m`：重置所有样式
- `\u001B[1m`：粗体
- `\u001B[31m`：红色前景
- `\u001B[42m`：绿色背景

#### 工具方法

| 方法 | 说明 |
|------|------|
| `color(text, colorCode)` | 给文本上色 |
| `style(text, codes...)` | 给文本应用多种样式 |
| `title(text)` | 生成粗体标题 |
| `success(text)` | 生成绿色成功消息 |
| `error(text)` | 生成红色错误消息 |
| `warning(text)` | 生成黄色警告消息 |
| `info(text)` | 生成蓝色信息消息 |
| `repeat(s, count)` | 重复字符串 |
| `clearScreen()` | 清除屏幕 |
| `getVisibleLength(s)` | 获取字符串可见长度（中文算2） |
| `centerText(text, width)` | 居中显示文本 |
| `getProgressBar(progress, width)` | 生成进度条 |
| `getAsciiTitle()` | 生成 ASCII 艺术字标题 |

#### 边框字符

使用 Unicode 边框字符绘制表格和面板：
- 粗边框：`━ ┃ ┏ ┓ ┗ ┛ ┣ ┫ ┳ ┻ ╋`
- 轻边框：`─ │ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼`
- 双线边框：`═ ║ ╔ ╗ ╚ ╝ ╠ ╣ ╦ ╩ ╬`

---

### 4.2 ConsoleUI.java - 控制台交互界面

**文件路径**：`src/ui/ConsoleUI.java`

**核心作用**：提供员工管理系统的菜单式命令行操作界面，是系统的主要交互入口之一。

#### 类结构

```java
public class ConsoleUI {
    private final EmployeeService service;    // 员工服务
    private final LogService logService;      // 日志服务
    private final Scanner sc;                 // 键盘输入
    private final LanguageService lang;       // 语言服务
}
```

#### 主菜单循环

`start()` 方法：
1. 打印主菜单（Dashboard 风格）
2. 读取用户输入
3. 根据选择调用对应功能方法
4. 执行完毕后暂停等待用户按回车
5. 循环直到用户选择退出（99）

#### 菜单选项（0-24/99）

| 编号 | 功能 | 说明 |
|------|------|------|
| 0 | 切换语言 | 中英文切换 |
| 1 | 添加员工 | 选择类型，输入信息 |
| 2 | 删除员工 | 按工号删除，二次确认 |
| 3 | 修改员工 | 按工号修改信息 |
| 4 | 查询员工 | 多种查询方式 |
| 5 | 浏览全部 | 分页显示 |
| 6 | 计算薪资 | 按工号计算 |
| 7 | 部门统计 | 各部门人数 |
| 8 | 薪资排序 | 从高到低 |
| 9 | 薪资筛选 | 按范围筛选 |
| 10 | 操作日志 | 显示操作记录 |
| 11 | 设置绩效 | 设置绩效等级 |
| 12 | 导出CSV | 导出到文件 |
| 13 | 薪资柱状图 | 控制台柱状图 |
| 14 | 撤销操作 | 撤销上一步 |
| 15 | 薪资冠军 | 最高/最低薪资 |
| 16 | 涨薪管理 | 个人/部门涨薪 |
| 17 | 请假扣薪 | 请假扣薪计算 |
| 18 | 修改密码 | 修改登录密码 |
| 19 | 数据备份 | 备份到文件 |
| 20 | 员工转岗 | 转换员工类型 |
| 21 | 从备份恢复 | 恢复数据 |
| 22 | 批量导入 | 从文件导入 |
| 23 | 月度报表 | 生成报表 |
| 24 | 入职趋势图 | 入职趋势 |
| 99 | 退出系统 | 自动备份后退出 |

#### 表格显示

使用 Unicode 边框字符绘制表格，支持：
- 7列显示：工号、姓名、部门、类型、绩效、工龄、薪资
- 各列使用不同颜色
- 绩效等级使用背景色（A=绿、B=黄、C=红）
- 薪资使用绿色右对齐

#### 分页浏览

`listAllEmployees()` 方法支持分页显示：
- 每页显示 10 条记录
- 支持 p（上一页）/ n（下一页）/ q（退出）导航

---

### 4.3 LoadingAnimation.java - 加载动画

**文件路径**：`src/ui/LoadingAnimation.java`

**核心作用**：在用户登录成功后显示一个美观的加载动画效果。

#### 动画效果

- 旋转动画字符：`◐ ◓ ◑ ◒`
- 进度条：使用 `█` 和 `░` 字符
- 动态加载提示文字（7种不同提示）
- 蓝色背景边框

#### 关键实现

```java
for (int i = 0; i <= totalSteps; i++) {
    // 使用 \r 回到行首实现动画效果
    System.out.print("\r");
    // 绘制进度条和旋转图标
    // 模拟加载延时
    Thread.sleep(80 + (int)(Math.random() * 60));
}
```

- 使用 `\r`（回车符）回到行首，在同一行更新进度
- 随机延时（80-140ms）模拟真实加载过程

---

### 4.4 LoginUI.java - Swing登录界面

**文件路径**：`src/ui/LoginUI.java`

**核心作用**：提供现代化的深色主题登录界面，风格参考 Claude/ChatGPT 的简洁高级 UI。

#### 界面特点

- 深色主题（背景 #121218）
- 圆角卡片设计
- 渐变进度条
- 输入框焦点动画
- 登录成功淡入动画
- 密码错误抖动效果
- 窗口拖动支持

#### 关键组件

| 组件 | 说明 |
|------|------|
| 用户名输入框 | 带占位符和焦点高亮 |
| 密码输入框 | 隐藏输入，支持回车登录 |
| 登录按钮 | 自定义绘制，hover 发光效果 |
| 进度条 | 渐变色，登录成功后显示 |
| 成功标签 | 淡入动画显示 ✓ |

#### 登录流程

1. 用户输入用户名和密码
2. 从 `config.txt` 读取存储的密码
3. 验证密码
4. 登录成功：启动进度动画 → 显示成功标签 → 关闭登录界面 → 进入主程序
5. 登录失败：输入框抖动，显示错误提示

---

## 5. 现代化UI层（Modern UI）

### 5.1 Theme.java - 主题配置

**文件路径**：`src/ui/modern/Theme.java`

**核心作用**：定义全局深色主题的颜色、字体、尺寸等常量，提供统一的 UI 组件创建方法。

#### 颜色系统

| 类别 | 颜色 | 用途 |
|------|------|------|
| 背景 | `BG_DARK` (#1E1E1E) | 主背景 |
| | `BG_SIDEBAR` (#252525) | 侧边栏背景 |
| | `BG_CARD` (#2D2D2D) | 卡片背景 |
| | `BG_INPUT` (#333333) | 输入框背景 |
| 强调色 | `ACCENT_PRIMARY` (#0078D4) | 主色调（蓝色） |
| | `ACCENT_SECONDARY` (#7C4DFF) | 次要色调（紫色） |
| | `ACCENT_GREEN` (#4CAF50) | 成功/薪资 |
| | `ACCENT_RED` (#F44336) | 危险/错误 |
| | `ACCENT_ORANGE` (#FF9800) | 警告/冠军 |
| 文字 | `TEXT_PRIMARY` (#FFFFFF) | 主文字（白色） |
| | `TEXT_SECONDARY` (#B0B0B0) | 次要文字 |
| | `TEXT_MUTED` (#808080) | 弱化文字 |
| 边框 | `BORDER` (#3C3C3C) | 边框颜色 |

#### 字体系统

| 字体 | 大小 | 粗细 | 用途 |
|------|------|------|------|
| `FONT_TITLE` | 22 | Bold | 页面标题 |
| `FONT_H1` | 18 | Bold | 一级标题 |
| `FONT_H2` | 15 | Bold | 二级标题 |
| `FONT_H3` | 13 | Bold | 三级标题 |
| `FONT_BODY` | 13 | Plain | 正文 |
| `FONT_SMALL` | 12 | Plain | 小字 |

所有字体统一使用"微软雅黑"。

#### 组件工厂方法

| 方法 | 说明 |
|------|------|
| `createCard()` | 创建圆角卡片面板 |
| `createButton(text)` | 创建主按钮（蓝色） |
| `createSecondaryButton(text)` | 创建次要按钮（带边框） |
| `createDangerButton(text)` | 创建危险按钮（红色） |
| `createTextField()` | 创建输入框 |
| `createScrollPane(view)` | 创建美化滚动面板 |

---

### 5.2 ModernLoginView.java - 现代化登录窗口

**文件路径**：`src/ui/modern/ModernLoginView.java`

**核心作用**：提供独立的现代化登录窗口，支持用户名和密码输入，最多3次尝试机会。

#### 界面特点

- 无边框窗口（`setUndecorated(true)`）
- 顶部渐变条
- 用户图标（👤）
- 密码使用 `●` 隐藏显示
- 错误提示和剩余次数显示

#### 登录验证

```java
private void doLogin() {
    // 1. 验证用户名和密码不为空
    // 2. 从 config.txt 读取存储的密码
    // 3. 验证密码
    // 4. 成功：关闭窗口，调用回调
    // 5. 失败：增加尝试次数，显示剩余次数
    // 6. 超过3次：2秒后退出程序
}
```

#### 同步等待机制

```java
public static boolean showLogin() {
    ModernLoginView loginView = new ModernLoginView();
    loginView.setVisible(true);
    synchronized (loginView) {
        while (loginView.isVisible()) {
            try {
                loginView.wait();  // 等待窗口关闭
            } catch (InterruptedException ignored) {}
        }
    }
    return loginView.isLoginSuccess();
}
```

- 使用 `wait()/notifyAll()` 机制实现同步等待
- 在 `dispose()` 方法中调用 `notifyAll()` 唤醒等待线程

---

### 5.3 MainDashboard.java - 主仪表盘

**文件路径**：`src/ui/modern/MainDashboard.java`

**核心作用**：主仪表盘窗口，左侧侧边栏放功能按钮，右侧主区域显示操作内容。

#### 窗口布局

```
┌─────────────────────────────────────────────────┐
│  📊 员工管理系统                    🕐 2026-06-04 │  ← 顶部标题栏
├──────────┬──────────────────────────────────────┤
│ 👥 员工管理 │                                    │
│   📋 员工列表 │         内容区域                  │
│   ➕ 添加员工 │    (动态切换功能面板)              │
│   🔍 查询员工 │                                    │
│   🔄 员工转岗 │                                    │
│          │                                    │
│ 💰 薪资管理 │                                    │
│   💵 计算薪资 │                                    │
│   📈 薪资排序 │                                    │
│   ...      │                                    │
│          │                                    │
│ 📊 统计报表 │                                    │
│   🏢 部门统计 │                                    │
│   ...      │                                    │
│          │                                    │
│ ⚙ 系统功能  │                                    │
│   📝 操作日志 │                                    │
│   ...      │                                    │
├──────────┴──────────────────────────────────────┤
│ 👥 员工总数：X 人                    就绪         │  ← 底部状态栏
└─────────────────────────────────────────────────┘
```

#### 侧边栏分组

| 分组 | 功能按钮 |
|------|---------|
| 👥 员工管理 | 员工列表、添加员工、查询员工、员工转岗 |
| 💰 薪资管理 | 计算薪资、薪资排序、薪资筛选、薪资冠军、涨薪管理、请假扣薪 |
| 📊 统计报表 | 部门统计、薪资柱状图、入职趋势、月度报表 |
| ⚙ 系统功能 | 操作日志、导出CSV、批量导入、数据备份、恢复备份、修改密码 |

#### 面板切换机制

```java
private void showPanel(String cmd) {
    contentPanel.removeAll();
    switch (cmd) {
        case "employee":
            // 显示员工